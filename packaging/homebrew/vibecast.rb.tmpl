# typed: false
# frozen_string_literal: true

# Generated by the vibecast release pipeline (release.yml -> homebrew job).
# Do not edit by hand; changes are overwritten on the next release.
class Vibecast < Formula
  desc "Native Google Cast receiver"
  homepage "https://github.com/emilsvennesson/vibecast"
  version "__VERSION__"
  license "MIT"

  on_macos do
    # Apple Silicon only (Intel macOS is unsupported).
    on_arm do
      url "__BASE__/vibecast-__TAG__-aarch64-apple-darwin.tar.gz"
      sha256 "__SHA_MACOS__"
    end
  end

  on_linux do
    on_arm do
      url "__BASE__/vibecast-__TAG__-aarch64-unknown-linux-gnu.tar.gz"
      sha256 "__SHA_LINUX_ARM__"
    end
    on_intel do
      url "__BASE__/vibecast-__TAG__-x86_64-unknown-linux-gnu.tar.gz"
      sha256 "__SHA_LINUX_AMD__"
    end
  end

  def install
    bin.install "vibecast"
  end

  test do
    assert_match "vibecast", shell_output("#{bin}/vibecast --help")
  end
end
